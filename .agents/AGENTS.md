<!-- BEGIN:package-manager-rule -->
# Package Manager

Always use `pnpm` as the package manager for this project. Do not use `npm` or `yarn`. 
When running scripts, adding dependencies, or doing any package management tasks, default to `pnpm`.
<!-- END:package-manager-rule -->
